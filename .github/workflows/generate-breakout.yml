name: Generate Breakout SVG

on:
  schedule:
    - cron: "0 0 * * *"  # Run daily at midnight
  workflow_dispatch:     # Allow manual triggers

jobs:
  generate-svg:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate SVG
        uses: cyprieng/github-breakout@v1.2.0
        with:
          github_username: Sheriffelrefaey
          enable_ghost_bricks: true
          output_path: ./output/
          paddle_color: "#1F6FEB"
          ball_color: "#1F6FEB"
          bricks_colors: "#ebedf0,#9be9a8,#40c463,#30a14e,#216e39"

      - name: Check output files
        run: |
          echo "Checking output directory:"
          ls -la ./output/ || echo "Output directory doesn't exist"

      - name: Move generated SVGs to temp
        run: |
          mkdir -p /tmp/breakout-images
          if [ -f output/light.svg ]; then
            mv output/light.svg /tmp/breakout-images/breakout-light.svg
          else
            echo "light.svg not found"
          fi
          if [ -f output/dark.svg ]; then
            mv output/dark.svg /tmp/breakout-images/breakout-dark.svg
          else
            echo "dark.svg not found"
          fi

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and force-push SVGs to target branch
        run: |
          git checkout --orphan github-breakout
          git rm -rf .
          mkdir images
          mv /tmp/breakout-images/* images/
          git add images
          git commit -m "chore: update GitHub breakout SVGs" || echo "No changes to commit"
          git push --force origin github-breakout